# VESTIGE_TEMPLATE_MARKER=unconfigured
# This file was auto-seeded on first launch. Docker services will start fine
# with these defaults for local-only use, but you should still open this file
# once to see what's configurable (LLM keys, etc).

POSTGRES_USER=vestige
POSTGRES_PASSWORD=vestige_local_default

SETTINGS_ENCRYPTION_KEY=

LLM_DISCOVERY_ENABLED=false
LLM_MODE=direct
SELECTOR_API_BASE=
SELECTOR_API_KEY=
SELECTOR_MODEL=
DIRECT_API_BASE=
DIRECT_API_KEY=
DIRECT_MODEL=

SPRING_JPA_HIBERNATE_DDL_AUTO=validate